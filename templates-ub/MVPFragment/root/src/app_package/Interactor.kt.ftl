package ${escapeKotlinIdentifiers(packageName)}.interactors

import ${escapeKotlinIdentifiers(packageName)}.repositories.${interfaceRepository} 

class ${interactor}(private val repository: ${interfaceRepository}) {

}
